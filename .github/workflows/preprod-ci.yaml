name: PreProd Branch CI

on:
  workflow_dispatch:
  push:
    branches:
      - 'main'
    paths-ignore:
      - 'deploy/**'
      - '**/*.md'
      - 'versions.json'
      - 'doc/**'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  call-ci:
    name: Call PreProd-CI Workflow
    uses: mdefenders/shared-workflows/.github/workflows/preprod-ci.yaml@dev
    secrets: inherit